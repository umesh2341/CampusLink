# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (PostgreSQL)
DB_USER=postgres
DB_PASSWORD=your_secure_password
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=campuslink_db

# External Connection String (optional, e.g. for production hosting like Supabase/Render)
# DATABASE_URL=postgresql://user:password@host:port/database
